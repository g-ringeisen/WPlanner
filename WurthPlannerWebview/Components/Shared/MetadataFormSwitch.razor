@namespace WurthPlannerWebview.Components.Shared
@using WurthPlanner.Models

@* Les métadonnées typées n'ont pas encore de contrat dans le domaine actuel. *@
@if (WorkItem is not null)
{
    <p class="text-muted mb-0">Aucune métadonnée spécifique n'est configurée pour ce type d'élément.</p>
}

@code {
    [Parameter, EditorRequired] public IWorkItem? WorkItem { get; set; }
}
